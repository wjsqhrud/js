import { data } from "./data.js";

const hospitalList = document.querySelector(".hospital__container");

// const image = data.map(({ titleImage }) => ({titleImage}));

// const hospital = data.map(
//   ({
//     hospitalSubway,
//     hospitalName,
//     title,
//     rating,
//     ratingCount,
//     cost,
//     currency,
//   }) => `({
//   hospitalSubway,
//   hospitalName,
//   title, rating, ratingCount,cost,  currency
// })`
// );

// // const price = data.map(({ cost, currency }) => ({ cost, currency }));
